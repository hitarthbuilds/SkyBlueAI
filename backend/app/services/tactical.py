from typing import Dict, Any


def simulate_tactical_adjustments(context: Dict[str, Any]) -> Dict[str, Any]:
    return {
        "recommendations": [
            "Trigger press when opponent fullback receives with closed body shape.",
            "Use 3-2 build-up to isolate wide winger 1v1.",
            "Overload left half-space before switching to weak side.",
        ],
        "confidence": 0.78,
    }
